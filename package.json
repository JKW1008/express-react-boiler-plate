{
  "name": "yj-stamp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "cd client && npm ci && npm run build && rm -rf ../server/build && mv build ../server",
    "start": "cd server && npm ci && node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "framer-motion": "^10.16.1",
    "react-icons": "^4.10.1",
    "react-multi-carousel": "^2.8.4",
    "react-responsive-carousel": "^3.2.23"
  }
}
